<!doctype html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Вырежите слово 'bbb' тремя разными способами</title>
</head>
<body>
  <script>
	let str = "aaa bbb ccc";
	alert('через substr: ' + str.substr(str.indexOf(' ') + 1, 3));
	alert('через substring: ' + str.substring(str.indexOf(' ') + 1, str.lastIndexOf(' ')));
	alert('через slice: ' + str.slice(str.indexOf(' ') + 1, str.lastIndexOf(' ')));	
  </script>
</body>
</html>
